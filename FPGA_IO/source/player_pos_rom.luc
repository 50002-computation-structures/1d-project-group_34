module player_pos_rom (
    input map_num[2],
    output outx[2],
    output outy[2]
  ) {
  const P_POS_ROM = {b0011, b1100, b0000//index 0 is rightmost
  };
  always {
    outx = P_POS_ROM[map_num][1:0];
    outy = P_POS_ROM[map_num][3:2];
  }
}
